import CustomLabel from "../../pages/Common/CustomLabel";
import { render } from '@testing-library/react';

test('renders learn react link', () => {
    render(<CustomLabel  title={"Most Common"}/>)
});